//Problem no. 1773-->Count Items Matching a Rule

class Solution {
    public int countMatches(List<List<String>> items, String ruleKey, String ruleValue) {
        int count = 0;
        int col = 0;
        if(ruleKey.equals("color")){
            col = 1;
        }
        if(ruleKey.equals("name")){
            col = 2;
        }
        for(int row = 0; i < items.size(); row++){
            if(item.get(row).get(col).equals(ruleKey)){
                count++;
            }
        }
        return count;
    }
}
