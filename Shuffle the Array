 problem no. 1470 -> Shuffle the Array

public int[] shuffle(int[] nums, int n) {
        int[] shuffle = new int[nums.length];
        for(int i = 0; i < nums.length; i++){
           if(i % 2 == 0){
            shuffle[i] = nums[ i / 2]; 
           } else {
            shuffle[i] = nums[n + (i/2)];
           }
        }
        return shuffle;
    }
